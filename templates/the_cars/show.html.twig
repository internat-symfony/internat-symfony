{% extends 'base.html.twig' %}

{% block title %}Cars{% endblock %}

{% block body %}
    <h1>Cars</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ car.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ car.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ car.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_the_cars_index') }}">back to list</a>

    <a href="{{ path('app_the_cars_edit', {'id': car.id}) }}">edit</a>

    {{ include('the_cars/_delete_form.html.twig') }}
{% endblock %}
